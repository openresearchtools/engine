Place upstream `llama.cpp` here (symlink, submodule, or checkout).

Expected by build script default:
- `build/build_bridge.ps1` uses `third_party/llama.cpp` unless overridden.
- bridge patch sources are copied from `../bridge` into `third_party/llama.cpp/MARKDOWN/bridge`.

